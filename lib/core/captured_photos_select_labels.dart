/// Optional labels for **Captured Pictures → Select… → unassigned / generic rows**.
///
/// Leave a line empty (or `''`) to use the built-in English or Filipino text.
/// Edit this file to match your project wording (e.g. a specific field name or process).
library;

/// Title when app language is **English** (empty = default: `Only "Field" rows`).
const String capturedPhotosSelectUnassignedTitleEn = '';

/// Subtitle when app language is **English**
/// (empty = default: `Items still using the generic Field label`).
const String capturedPhotosSelectUnassignedSubtitleEn = '';

/// Title when app language is **Filipino** (empty = default: `Mga "Field" lang`).
const String capturedPhotosSelectUnassignedTitleFil = '';

/// Subtitle when app language is **Filipino**.
const String capturedPhotosSelectUnassignedSubtitleFil = '';
